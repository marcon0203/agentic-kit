# 项目铁律

- **默认不自动运行语法检查、类型检查、测试或构建。** 只有在用户明确要求验证，
  或改动的风险确实需要验证时才运行；此时按改动范围选择最小充分的命令
  （例如只 `go build`/`go vet` 受影响的包，只跑相关的 `go test`/`vitest`
  文件，而不是无条件跑全仓库的 `go build ./...`、`npm run build`、
  `npm run test` 等全量命令）。是否验证过、验证了什么、结果如何，都要在
  交付说明里如实写清楚，不要默认全跑也不要假装跑过。
