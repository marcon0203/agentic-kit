# Template only — placeholder values, never commit real secrets here.
# In practice this Secret is created out-of-band (kubectl create secret,
# a sealed-secrets/external-secrets controller, etc.), not applied as-is.
apiVersion: v1
kind: Secret
metadata:
  name: agentic-kit-server-secrets
  namespace: agentic-kit
type: Opaque
stringData:
  DATABASE_URL: "postgres://REPLACE_ME:REPLACE_ME@REPLACE_ME:5432/agentic_kit?sslmode=require"
  # openssl rand -base64 32
  JWT_SECRET: "REPLACE_ME"
  # openssl rand -base64 32 — must decode to exactly 32 raw bytes (AES-256)
  CREDENTIAL_AES_KEY: "REPLACE_ME"
