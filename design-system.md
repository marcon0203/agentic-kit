# AI Agent 平台 — 全局设计系统

> **权威来源**：本文档所有数值来自 `ui-design-system/references/tokens-pc.md`（PC/桌面端）。
> 本平台是后台控制台形态，**只使用 PC 端设计系统**，不涉及移动端 tokens。
> 任何页面不得在本地重新定义色值、字号、容器宽度——需要新值时按方法论推导后合并回 tokens 源文件。

---

## 设计取向（依据明确的偏好确认）

三条已确认的偏好，本文档所有细则围绕它们展开：

| 偏好 | 选择 | 落地方式 |
|---|---|---|
| **界面密度** | 宽松呼吸型（类 Notion / Stripe） | 正文默认取 `body-lg` 15px 而非 14px；卡片内边距取 `space-7` 32px 而非 24px；模块间距下限提到 `space-10` 64px；表格行高 56px 而非紧凑的 44px |
| **视觉风格** | 适度精致 | 关键交互有过渡动画（150–250ms）；品牌渐变用于主按钮和关键选中态；卡片 hover 有位移 + 阴影层次；但不做装饰性插画和大面积渐变背景 |
| **面向对象** | 要能给领导/客户演示 | 首屏观感优先：广场页、Chat 页、工作台三处需经得起投屏；空态不能显得"没做完"，要有引导性文案而非一句冷冰冰的「暂无数据」 |

**"宽松"不等于"松散"**：留白是为了让信息分组更清楚，不是把同一组信息拉开。同组元素仍用 `space-2`~`space-3` 紧贴，组与组之间才拉开到 `space-7` 以上——留白的作用是**区分层次**，不是均匀铺开。

---

## 零、两个必须先对齐的偏差

前期 POC 原型和架构文档在设计系统确立之前编写，与 tokens 存在两处冲突，**以本文档为准**：

| 冲突项 | 上游文档的写法 | 按设计系统应为 | 影响的 spec |
|---|---|---|---|
| **整体色调** | 深色控制台风格（`--bg-canvas: #0f1214` 等自定义深色变量） | **浅色**：页面底 `surface-page #f6f7fb`，卡片 `surface #ffffff` | spec-13、spec-14、spec-15、spec-16、spec-17 |
| **登录注册形态** | 独立路由页 + 路由守卫 | **认证弹窗**：全局身份组件，以 modal 形式存在于 app shell 内，**从不是独立路由** | spec-13 |

深色模式：`tokens-pc.md` 未定义深色映射，本产品**V1 不做深色模式**。若后续要做，需按 `color.md` 的要求为每个语义 token 补一套深色映射并**单独重跑一遍对比度检查**（不能假设浅色达标深色也达标），完成后合并回 tokens 源文件。

---

## 一、色板

### 1.1 基础色板（引用自 tokens）

| Token | 值 | 用途 |
|---|---|---|
| `colors.primary` | `#4264ff` | 品牌蓝：链接、信息高亮、焦点态、品牌渐变起点 |
| `colors.secondary` | `#8b5cff` | 紫：渐变终点、AI 能力相关强调、关键选中态 |
| `colors.tertiary` | `#63d8ff` | 青：轻量科技感点缀 |
| `colors.mint` | `#6ce0c7` | 有效/可用指示，与语义 success 区分 |
| `colors.success` | `#199572` | 成功状态、正向反馈 |
| `colors.warning` | `#d8862f` | 需要注意但非阻断 |
| `colors.error` | `#dc5970` | 错误、危险操作 |
| `colors.ink-900` | `#11162f` | 主文字 |
| `colors.ink-700` | `#4d5572` | 正文 |
| `colors.ink-500` | `#7d849e` | 元信息、禁用文字 |
| `colors.navy-950` / `navy-900` | `#090d2b` / `#111746` | 深色页面背景、页脚、**认证弹窗** |
| `colors.surface` | `#ffffff` | 卡片、弹窗表面 |
| `colors.surface-page` | `#f6f7fb` | 页面背景 |
| `colors.surface-muted` | `#f1f4fa` | 标签底色等弱填充 |
| `colors.border` / `border-strong` | `#e4e7f0` / `#cfd8e8` | 默认分隔线 / 强调与 hover 边框 |
| `colors.tab-text` / `tab-active` | `#64708b` / `#5861dc` | 标签页文字 / 激活态 |

**品牌渐变** `linear-gradient(128deg, #365fff 0%, #655eff 46%, #9858ee 100%)`：仅用于主按钮、关键选中态、特色视觉。**绝不用作大面积正文背景**。

### 1.2 本平台新增的语义映射（不新增色值）

平台特有的两组状态——**节点执行状态**和**运行状态**——直接复用既有语义色，不造新色：

| 平台状态 | 映射 token | 配套图标/文字（**必须**，颜色不能单独承载状态） |
|---|---|---|
| 等待中 pending | `colors.ink-500` + `surface-muted` 底 | 空心圆图标 + 文字「等待中」 |
| 运行中 running | `colors.primary` | 脉冲圆点 + 文字「运行中」 |
| 待审批 gate | `colors.warning` | 感叹号图标 + 文字「待审批」 |
| 已完成 done | `colors.success` | 对勾图标 + 文字「已完成」 |
| 失败 failed | `colors.error` | 叉号图标 + 文字「失败」 |
| 黑盒资源 blackbox | `colors.ink-700` + `surface-muted` 底 | 锁图标 + 文字「黑盒」 |

**推导说明**：待审批语义上是"需要注意但非阻断"，正好对应 `warning` 的既定语义；运行中是信息性高亮，对应 `primary`。这样既不稀释强调色的信号价值，也避免了造一套平行色阶。

### 1.3 Agent 角色区分（不用颜色承载）

Chat 界面里有多个 Agent 角色（产品经理/架构师/UI 设计师/全栈工程师），**不给每个角色分配一种颜色**——角色是用户自定义的，数量不可控，给每个角色配色会迅速耗尽色板并与状态色撞车。

角色区分方式：**头像圆形容器（`surface-muted` 底 + `ink-700` 首字）+ 角色名文字**。所有 Agent 气泡视觉一致，靠文字标识区分。

---

## 二、字体排版

| 层级 | Token | 字号/字重/行高 | 本平台用在哪 |
|---|---|---|---|
| 页面标题 | `headline-lg` | 44px / 700 / 1.2（衬线） | 广场首页等内容型页面标题 |
| 区块标题 | `headline-md` | 34px / 600 / 1.4（衬线） | 页面内大分区 |
| 面板标题 | `headline-sm` | 19px / 700 / 1.45（无衬线） | 卡片区块标题、表单分组标题 |
| 卡片标题 | `title-card` | 20px / 700 / 1.48（衬线） | 广场资源卡片标题 |
| 弹窗标题 | `title-modal` | 24px / 500（PingFang SC） | **所有弹窗**，弹窗绝不用衬线 |
| 正文大 | `body-lg` | 15px / 400 / 1.72 | 页面引导语、Chat 气泡正文 |
| 正文 | `body-md` | 14px / 400 / 1.72 | 表格、表单、按钮文字 |
| 正文小 | `body-sm` | 13px / 400 / 1.7 | 辅助说明 |
| 标签 | `label-md` | 13px / 600 / 1.4 | 表单标签 |
| 说明文字 | `caption` | 12px / 600 / 1.5 | 元信息、时间戳、helper text |
| 数据 | `data` | 30px / 800 / 1.1 | Token 消耗、订阅数等核心指标 |
| 眉标 | `eyebrow` | 12px / 800，字距 0.18em | 标题上方的小标签 |

**本平台的取值倾向（宽松呼吸型）**：

- **正文默认用 `body-lg` 15px**，不是 14px——`body-md` 14px 只用于表格单元格、按钮文字等需要紧凑的场景
- 面板标题优先 `headline-sm` 19px，避免 14px 粗体充当标题（层级会糊）
- 说明文字用 `caption` 12px，但**不要连续成段**——超过两行的说明升到 `body-sm` 13px

**硬性规则**：
- 中文正文不低于 12px，可操作元素文字不低于 13px
- 每个独立页面遵循一套层级：标题 → 引导语 → 面板标题 → 正文/数据 → 元信息，不在页面内局部重定义字号
- **等宽字体例外**：`run_id`、事件类型、DSL 源码、JSON 展示使用 `Manrope` 的等宽回退或系统等宽字体，字号沿用 `body-sm`/`caption` 层级——这是数据可读性需要，但不新增字号档位

---

## 三、间距 · 圆角 · 阴影

### 间距（4px 基准）

| 场景 | 本平台取值（宽松倾向） |
|---|---|
| 同组元素之间（标签与输入框、图标与文字） | `space-2`(8) ~ `space-3`(12)，**保持紧贴，不拉开** |
| 组件内部分区 | `space-5`(20) ~ `space-6`(24) |
| 卡片内边距 | **`space-7`(32)**（tokens 组件默认 24px，本平台取上限） |
| 表单分组之间 | `space-7`(32) ~ `space-8`(40) |
| 页面模块之间 | `space-10`(64) 起步，重点分隔用 `space-11`(80) |
| 栅格间隙 | `gutter` 24px |
| 表格行高 | 56px（不用紧凑的 44px） |

容器：**全平台统一 `container-app` 1380px**（最小 24px 侧边距），不得逐页重定义。

### 圆角

| Token | 值 | 用在哪 |
|---|---|---|
| `rounded.xs` | 8px | 标签、计数器、微控件 |
| `rounded.sm` | 12px | 按钮、输入框、图标容器 |
| `rounded.md` | 16px | 下拉菜单、筛选器 |
| `rounded.lg` | 22px | 卡片、面板 |
| `rounded.xl` | 28px | 特色卡片、弹窗 |
| `rounded.full` | 9999px | 主按钮、标签页、状态胶囊 |

### 阴影

层级主要靠**边框和表面对比**建立，阴影只留给真正浮起的元素，不用于区分平铺内容。

- `shadow-sm` — 输入框、轻量浮层
- `shadow-md` — 卡片 hover、下拉菜单
- `shadow-lg` — 弹窗、显著浮层

深色背景（navy 表面、认证弹窗）上降低阴影不透明度，避免脏灰边。

---

## 四、通用组件视觉规范

### 按钮

胶囊形，默认高 44px（大 52 / 小 36）。**一个操作组内只能有一个主按钮**，它是按钮中唯一使用品牌渐变的。

| 变体 | 规格 |
|---|---|
| `button-primary` | 品牌渐变底 + 白字 |
| `button-secondary` | 白底 + `ink-900` 字 + `border` 边框 |
| `button-danger` | `error` 底 + 白字，仅用于删除/登出/破坏性操作 |
| 禁用态 | `border` 底 + `ink-500` 字，去阴影，禁用指针——**不能只靠变色** |

本平台用法：「运行」「订阅」「发布」为主按钮；「取消」「驳回」为次按钮；「退订」「下架」为 danger。

### 表单

输入框高 48px，圆角 13px。标签用 `label-md`，helper 用 `caption`。焦点态加品牌蓝边框 + 4px 低透明度光环。

- `input-error`：红边框 **必须**搭配下方明确错误信息，不能只给边框
- `input-valid`：`mint` 边框，不加冗余的"成功"文字
- 校验时机：输入时、失焦时、前置操作前、提交时——**提交时重新校验**，不信任早先的通过状态

### 卡片

白底 + 1px `border` + `rounded.lg` + 轻阴影，内边距 24px。结构：封面/类型图标 → 分类 → 标题(`title-card`) → 2-3 行摘要 → 标签 → 指标或操作。

Hover 仅上移 4px + 提升阴影，**不整体缩放**。

### 标签页 Tabs

三级导航的唯一模式，42px 标准高（紧凑 38px）。一组内一个激活项，标签不换行（改横向滚动），2-6 个为宜，超出改用 Select。

**Tabs 只在同层切换内容，绝不用于跳转页面**。语义色不得挪用作激活态。

### 弹窗 Modal

圆角 26-30px，遮罩为 navy-black 64-72% + 模糊。Header/Body/Footer 视觉分明，**footer 固定两个操作**：左侧次要「取消」+ 右侧唯一主操作（按任务措辞，如「取消/保存」），**绝不只有一个孤零零的「关闭」**。标题只用 `title-modal`。

### 认证弹窗

登录注册是**全局身份组件，以弹窗形式存在于 app shell 内，绝不是独立路由**。610px 宽，深 navy 渐变底 + 中心柔光，仅内部滚动。每个视图一个通栏主提交按钮；第三方登录为次要描边按钮，不与之竞争。`Escape`、关闭控件、遮罩点击都能关闭且不改变认证状态。

### 状态胶囊 Chip

标签用 `surface-muted`/`ink-700`；**状态胶囊必须额外配图标或文字**，颜色永远不单独承载状态。Chip 不承载主操作。

### 指标 Metric

数值为主导元素（`data` 字体，800 字重），标签用 `caption`，趋势用语义色 + 方向符号。一行三个以上指标共享同一数值基线。

### 空态 / 加载态

- 空态用虚线边框或浅色面板，**必须同时说明原因和下一步动作**
- 骨架屏微光保持中性灰，**绝不用品牌渐变**
- 所有加载动效遵守 `prefers-reduced-motion`

### Toast

底部居中，距边 28px，停留 2-3 秒，同时只有一个。**只做反馈，不承载需要用户决策的内容**。

---

## 四点五、动效规范

"适度精致"的具体落点。**动效服务于状态变化的可理解性，不做纯装饰**。

### 时长与缓动

| 类型 | 时长 | 缓动 | 用在哪 |
|---|---|---|---|
| 微交互 | 150ms | `ease-out` | 按钮 hover、输入框聚焦、chip 状态切换 |
| 元素进出 | 200ms | `cubic-bezier(0.16, 1, 0.3, 1)` | 卡片 hover 上移、下拉展开、Toast 进出 |
| 弹窗/抽屉 | 250ms | 同上 | 弹窗淡入 + 轻微缩放（0.98→1）、抽屉滑入 |
| 内容流式 | 逐字 16–24ms | linear | Chat 气泡打字机效果 |
| 状态脉冲 | 1.6s 循环 | `ease-in-out` | 「运行中」「待审批」的呼吸点 |

**超过 300ms 的动效不做**——演示场景下等待感比精致感更容易被感知到。

### 具体规则

- **卡片 hover**：上移 4px + 阴影从 `shadow-sm` 到 `shadow-md`，**不缩放整卡**（缩放会带来抖动感）
- **按钮 hover**：主按钮渐变亮度 +4%，次按钮边框转 `border-strong`；均不改变尺寸，避免布局跳动
- **列表项进入**：新增行淡入 + 从 8px 下方上移，**不做逐条延迟的瀑布式动画**（列表长时会显得拖沓）
- **数字变化**：Token 消耗、订阅数等指标变化时做数值滚动过渡（400ms），这是少数值得做长一点的地方——演示时能吸引注意
- **骨架屏**：中性灰微光，1.5s 循环，**绝不用品牌渐变**
- **页面切换**：不做整页转场动画，只做内容区淡入（150ms）

### 无障碍

所有动效**必须**包在 `prefers-reduced-motion` 判断内：

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

注意：关闭动效后**状态本身仍要可辨识**——「运行中」的脉冲点停止动画后，仍需靠颜色 + 图标 + 文字表明状态，不能只剩一个静止的点。

---

## 五、图标

24×24 线性 SVG，`stroke-width: 1.8`（小对勾 2.6）。标准尺寸 18-21px，特色 24-28px，与相邻文字间距 8-10px。

**不要在同一上下文混用 emoji、图标字体和 SVG 图标**；纯图标控件必须有 `aria-label`。

---

## 六、无障碍

| 项 | 要求 |
|---|---|
| 对比度 | 正文 4.5:1；大字(≥18pt 或 ≥14pt 粗)/图标 3:1；UI 组件边框 3:1 |
| 焦点 | 3px 可见焦点轮廓，**永远不移除焦点环** |
| 点击区域 | 最小 40×40px |
| 键盘 | 完整支持 Tab / Enter / Space / Escape |
| 弹窗 | `role="dialog"` + `aria-modal` + 可读标题 |
| 表单错误 | 通过 `aria-describedby` 关联 |
| 图片 | 有意义的配 `alt` 描述，装饰性的 `alt=""` |
| 状态传达 | **任何状态都不能只靠颜色**，必须同时有文字、图标或形状变化 |

**本平台需额外注意的两处**：

1. **流式输出的屏幕阅读器播报**：Chat 气泡逐字追加内容时，若整段用 `aria-live="polite"` 会导致每追加一个字就重播一次。做法是气泡容器设 `aria-busy="true"`，**节点完成后**再置 `false` 并播报完整产出。
2. **执行图的键盘可达性**：SVG 执行图不能只支持鼠标 hover 查看节点详情，每个节点需可 Tab 聚焦、Enter 展开详情；同时提供「时间线」视图作为等价的纯文本替代路径。

---

## 七、命名约定

设计系统组件用 `ds-` 前缀；业务组件用 `模块-元素--状态`（如 `bundle-card__title`）。状态类统一 `is-active` / `is-open` / `is-loading` / `is-disabled`。

**新的可复用模式必须先加进设计系统再在页面使用**，不能反过来。

---

## 八、本平台新增组件（按 component-spec 方法论推导）

以下组件 tokens 未覆盖，按方法论推导。**新值需合并回 `tokens-pc.md`**。

### 组件：Agent 消息气泡 `ds-agent-bubble`

- **描述**：Chat 界面中承载单个 Agent 产出的容器，支持流式追加内容
- **适用平台**：PC
- **什么时候用**：Bundle 运行过程中展示某个 Agent 的思考与产出
- **什么时候不用**：用户自己的输入（用 `ds-user-bubble`，右对齐、`surface-muted` 底）；系统级提示（用 Toast 或内联提示条）

**结构**：角色头像(32px 圆) + 角色名(`label-md`) + 状态胶囊 + 正文区(`body-lg`) + 工具调用状态条(可选) + 底部操作区(「查看完整产出」链接)

**状态**：

| 状态 | 视觉 |
|---|---|
| 运行中 | 左边框 2px `primary`，状态胶囊「运行中」，正文逐字追加 + 末尾闪烁光标 |
| 已完成 | 左边框 `success`，胶囊「已完成」，显示「查看完整产出」链接 |
| 失败 | 左边框 `error`，胶囊「失败」，正文区改为错误信息（黑盒运行时已脱敏） |
| 黑盒 | 角色名后加「黑盒」chip，不显示思考过程，只显示最终产出 |
| 内容溢出 | 超过 12 行折叠，显示「展开全部」 |

**Token**：底色 `colors.surface`，边框 `colors.border`，圆角 `rounded.lg`，内边距 `space-5`，气泡间距 `space-4`

### 组件：执行图节点 `ds-graph-node`

- **描述**：右侧辅助栏中表示 Bundle 编排图里一个 Agent 节点
- **适用平台**：PC

**状态**：五种节点状态按 §1.2 的语义映射着色，**每种都配图标 + 文字**，不靠纯色区分。黑盒运行时渲染为占位节点（`surface-muted` 底 + 锁图标 + 「黑盒节点」文字，不显示真实节点名）。

**Token**：圆角 `rounded.sm`，节点内边距 `space-3`，节点间距 `space-6`；连线 `colors.border-strong` 1.2px，自环重试边用 `colors.warning` 虚线（3 3）

### 组件：内联审批卡 `ds-gate-card`

- **描述**：human gate 触发时插入 Chat 对话流的审批卡片
- **适用平台**：PC
- **为什么不用 Modal**：审批需要参考上下文对话内容做判断，弹窗会遮挡上下文；且弹窗打断阅读流，与"对话"的产品隐喻冲突

**结构**：警示图标 + 标题(「XX 已完成，需要你确认后继续」) + 关键产出摘要 + 操作区(「驳回」次按钮 + 「通过」主按钮)

**状态**：

| 状态 | 视觉 |
|---|---|
| 待处理（有权限） | `warning` 色边框 + 浅黄底，两个按钮可用 |
| 待处理（无权限） | 同上但按钮替换为文字「等待他人审批」 |
| 已通过 | 折叠为单行状态条 + `success` 对勾 |
| 已驳回 | 折叠为单行状态条 + `error` 叉号 |
| 超时 | 折叠为单行 + 说明超时处理结果（自动通过/驳回/中止） |

**Token**：边框 `colors.warning`，底色 `warning` 8% 透明度叠加在 `surface` 上，圆角 `rounded.md`，内边距 `space-5`

### 组件：广场资源卡 `ds-listing-card`

继承通用 `card` 规范，增加：作者信息行（头像 20px + 名称 + 版本号 + 订阅数）、类型 chip、右下角操作按钮（未订阅「订阅」主按钮 / 已订阅「进入使用」次按钮 + 「已订阅」chip）。

---

## 九、变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-21 | 初版。基于 `tokens-pc.md` 建立；修正上游深色风格与独立登录页两处偏差；新增 4 个平台特有组件规范 |
