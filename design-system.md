# AI Agent 平台 — 全局设计系统

> **权威来源**：本文档与 `web/src/index.css` 的 token 块一一对应，两者必须同时改。
> 本平台是后台控制台形态，PC 优先；移动端只保证**可用**（不横向溢出、焦点可见、动效可关），不做专门设计。
> 任何页面不得在本地重新定义色值、字号、容器宽度。

---

## 设计取向

产品的核心不是"一个有仪表盘的后台"，而是**一条会走动、会停下来等人的轨道**：多个 Agent 按图协作，
关键节点停在 human gate 等人点头，订阅者只能看到黑盒外壁。整套视觉围绕这件事来定，而不是套一层
通用 SaaS 皮。

| 取向 | 选择 | 落地方式 |
|---|---|---|
| **气质** | 工程制图台，不是营销落地页 | 暖白纸面 + 图纸蓝；小圆角（3–14px）；层级靠**分隔线和表面对比**建立，阴影只留给真正浮起的元素 |
| **注意力** | 一次只让一件事发亮 | **信号橙全站只有一个含义：需要你介入**。除 human gate 外任何地方不得使用；只有 gate 会自己动，其余元素只在 hover/focus 时有反馈 |
| **标识符** | 当作数据读，不当作正文读 | `ref` / `version` / `run_id` / 节点名 / DSL 一律等宽（`text-ref`），指标数字用 tabular 等宽对齐——平台上一半的文本是要逐字符比对和复制的字符串 |

### 签名元素：运行轨道（the run rail）

全站唯一"敢用力"的地方，也是唯一自带动效的东西：

- **首页 hero** —— 不是静态流程图，是一次真实推进：节点依次点亮，轨道逐段画出，**停在 human gate 呼吸**，之后的轨道保持灰色可见（要能看出接下来去哪，不只是走过哪）
- **运行页侧栏"执行图"** —— 同一套语言竖过来，节点是站点，中间的竖线是轨道
- **空态** —— 不用虚线方框，用一条"还没有东西经过"的空轨道
- **导航** —— 当前项是线上的一个站点（下划线），不是填色胶囊

其余界面一律克制：这条轨道读得出来，靠的是周围什么都不抢。

---

## 零、必须先对齐的偏差

前期 POC 原型和架构文档在设计系统确立之前编写，**以本文档为准**：

| 冲突项 | 上游文档的写法 | 按设计系统应为 |
|---|---|---|
| **整体色调** | 深色控制台风格（`--bg-canvas: #0f1214`） | **浅色**：页面底 `paper #fbfaf9`，卡片 `surface #ffffff` |
| **登录注册形态** | 独立路由页 + 路由守卫 | **认证弹窗**：全局身份组件，以 modal 形式存在于 app shell 内，**从不是独立路由** |
| **品牌渐变** | 紫蓝渐变用于主按钮与特色视觉 | **已废除**。渐变让每个界面看起来都像同一个 AI SaaS 模板；主按钮用实色 `blueprint` |

深色模式：V1 不做。若后续要做，需为每个语义 token 补一套深色映射并**单独重跑一遍对比度检查**。

---

## 一、色板

### 1.1 六个色 + 由它们派生的浅色

不引入第七个色相。需要"浅一档"时用下面已列出的 tint，不要临时调 opacity 造新色。

| Token | 值 | 用途 |
|---|---|---|
| `paper` | `#fbfaf9` | 页面底。微暖的白，不是奶油色——奶油底 + 衬线 + 陶土点缀是一套被用滥的默认组合 |
| `blueprint` | `#1f4e79` | 主色：结构、链接、主按钮、运行中 |
| `signal` | `#c2570f` | **唯一的注意力色：需要人介入**。human gate、待办、破坏性确认。其他地方一律不用 |
| `moss` | `#2e6b4f` | 完成、健康、已批准、已启用 |
| `rust` | `#a93636` | 失败、驳回、删除 |
| `graphite` | `#16181d` | 正文墨色（`ink-900`） |

派生 tint：`blueprint-tint #eef3f8`、`signal-tint #fdf2e9`、`moss-tint #edf5f1`、`rust-tint #fbeded`；
边框 `border #e6e2dc` / `border-strong #cdc7bd`；墨色分档 `ink-700 #4a505c`、`ink-500 #767d8b`。

### 1.2 平台语义映射（不新增色值）

| 语义 | 映射到 | 出现在 |
|---|---|---|
| `status-pending` | `ink-500` | 未开始的节点 |
| `status-running` | `blueprint` | 运行中的节点与运行 |
| `status-gate` | `signal` | human gate 待审批 |
| `status-done` | `moss` | 已完成 |
| `status-failed` | `rust` | 失败 |
| `status-blackbox` | `ink-700` | 黑盒资源 |

**颜色从不单独承载状态**：每处状态都是「色点 / 图标 + 文字」，色盲用户看到的信息量一致。

### 1.3 Agent 角色区分（不用颜色承载）

角色靠**名字本身**区分——节点名是等宽的 `ref`，本来就唯一且可读。不给 Agent 分配色相：
一次编排可能有十几个节点，十几种颜色只会把 `signal` 的含义冲淡。

## 二、字体排版

三种字体，三种职责。分工是**内容驱动**的，不是装饰：

| 字体 | 职责 | 为什么是它 |
|---|---|---|
| **Space Grotesk** | 标题（display） | 几何、末端有切口，读起来像制图与仪表，不像通用 SaaS 的 Inter/Poppins |
| **Inter + Noto Sans SC** | 正文，中英文 | 中文回落到 Noto Sans SC；标题遇中文同样回落——Space Grotesk 无中文字形，这是预期行为 |
| **JetBrains Mono** | **所有标识符与数字** | 平台上一半文本是 `ref`/`version`/`run_id`/节点名/DSL，等宽让它们可扫描、可逐字符比对、可按列对齐 |

| 层级 | class | 字号/字重/行高 | 用在哪 |
|---|---|---|---|
| 首屏标题 | `text-display-xl` | 52 / 700 / 1.06，字距 -0.03em | 仅首页 hero |
| 页面标题 | `text-display-lg` | 34 / 600 / 1.15 | `PageHeader` 的 h1 |
| 区块标题 | `text-display-md` | 22 / 600 / 1.25 | 卡片标题、弹窗标题 |
| 面板标题 | `text-display-sm` | 16 / 600 / 1.35 | `Section` 的 h2 |
| 正文大 | `text-body-lg` | 15 / 400 / 1.68 | 页面引导语、Chat 气泡 |
| 正文 | `text-body-md` | 14 / 400 / 1.65 | 表格、表单、按钮 |
| 正文小 | `text-body-sm` | 13 / 400 / 1.6 | 辅助说明 |
| 标签 | `text-label-md` | 13 / 600 / 1.4 | 表单标签 |
| 说明 | `text-caption` | 12 / 500 / 1.45 | 元信息、时间戳 |
| **标识符** | `text-ref` / `text-ref-lg` | 12.5 / 14（等宽） | ref、version、run_id、节点名、事件类型 |
| **指标** | `text-figure` | 26（等宽 tabular） | Token 消耗、成本、耗时 |
| 眉标 | `text-eyebrow` | 11（等宽），字距 0.16em | `PageHeader` 上方的英文小标签 |

**硬性规则**：
- 中文正文不低于 12px，可操作元素文字不低于 13px
- 眉标只放**拉丁字母**——11px 等宽 + 0.16em 字距下中文会糊
- 每个页面遵循一套层级，不在页面内局部重定义字号

## 三、间距 · 圆角 · 阴影

### 间距（4px 基准）

| 场景 | 本平台取值（宽松倾向） |
|---|---|
| 同组元素之间（标签与输入框、图标与文字） | `space-2`(8) ~ `space-3`(12)，**保持紧贴，不拉开** |
| 组件内部分区 | `space-5`(20) ~ `space-6`(24) |
| 卡片内边距 | `space-5`(20) ~ `space-6`(24) |
| 表单分组之间 | `space-7`(32) ~ `space-8`(40) |
| 页面模块之间 | `space-10`(64) 起步，重点分隔用 `space-11`(80) |
| 栅格间隙 | `gutter` 24px |
| 表格行高 | 48px |

容器：**全平台统一 `container-app` 1320px**（最小 24px 侧边距），不得逐页重定义。

### 圆角

小而一致——这是仪表，不是玩具。

| Token | 值 | 用在哪 |
|---|---|---|
| `rounded-xs` | 3px | `Ref` 标识符底、微控件 |
| `rounded-sm` | 5px | 按钮、输入框（shadcn 控件默认） |
| `rounded-md` | 7px | 下拉菜单、筛选器 |
| `rounded-lg` | 10px | 卡片、面板、列表容器 |
| `rounded-xl` | 14px | 弹窗 |
| `rounded-full` | 9999px | 状态圆点、头像、轨道站点 |

### 阴影

层级主要靠**边框和表面对比**建立，阴影只留给真正浮起的元素，不用于区分平铺内容。

- `shadow-sm` — 输入框、轻量浮层
- `shadow-md` — 卡片 hover、下拉菜单
- `shadow-lg` — 弹窗、显著浮层

深色表面（认证弹窗）上降低阴影不透明度，避免脏灰边。

---

## 四、通用组件视觉规范

### 按钮

`rounded-sm`（5px），默认高 36px（大 44 / 小 32）。**一个操作组内只能有一个主按钮**。

| 变体 | 规格 |
|---|---|
| `default` | `blueprint` 实底 + 白字。**不用渐变**——渐变让每个界面都像同一个 AI SaaS 模板 |
| `outline` | 白底 + `ink-900` 字 + `border-strong` 边框 |
| `ghost` | 无边框，hover 才出底色。用于列表行内的次要操作（删除、退订） |
| `destructive` | `rust` 底 + 白字，仅用于破坏性操作 |
| 禁用态 | 降透明度 + 禁用指针，并**保留 title 说明为什么不能点** |

审批按钮（通过/驳回）强制 `size="lg"`（44px）：这是一个决定，目标不该小到会误点。

### 表单

输入框高 36px，`rounded-sm`。标签用 `label-md`，helper 用 `caption`。焦点态见「六、无障碍」。

- `input-error`：`rust` 边框 **必须**搭配下方明确错误信息，不能只给边框
- `input-valid`：`moss` 边框，不加冗余的"成功"文字
- 校验时机：输入时、失焦时、提交时——**提交时重新校验**，不信任早先的通过状态

### 卡片与面板

白底 + 1px `border` + `rounded-lg`，内边距 20–24px，**默认无阴影**。层级靠边框和表面对比建立。
Hover 只把边框加深到 `border-strong`，不上移、不加阴影、不缩放。

列表优先用**一个容器 + 内部分隔线**，而不是 N 张独立卡片——后者会把一份列表读成一堆碎片。

### 标签页（`TabRail`）

**下划线，不是填色胶囊**。胶囊读起来像按钮，会诱使人点击自己正在看的那一页；下划线是位置标记，
而位置标记正是 tab 的语义。一组内一个激活项，2–6 个为宜，超出改用 Select。

**Tabs 只在同层切换内容，绝不用于跳转页面**。语义色不得挪用作激活态。

### 弹窗 Modal

`rounded-xl`，遮罩为深墨 64–72% + 模糊。**footer 固定两个操作**：左侧次要「取消」+ 右侧唯一主操作
（按任务措辞，如「取消/保存」），**绝不只有一个孤零零的「关闭」**。

### 认证弹窗

登录注册是**全局身份组件，以弹窗形式存在于 app shell 内，绝不是独立路由**。
`Escape`、关闭控件、遮罩点击都能关闭且不改变认证状态。

### 状态显示

**颜色永远不单独承载状态**：状态 = 色点/图标 + 文字。
`StatusChip` 无底色，只用图标 + 文字 + 语义色；列表行用 `size-2` 圆点 + 右对齐文字。

### 标识符（`Ref`）

`ref` / `version` / `run_id` / 节点名一律走 `Ref` 组件：等宽、`rounded-xs`、浅底。
它和正文在视觉上必须能一眼分开——这些是要逐字符比对和复制的字符串。

### 指标（`Figure` / `FigureRow`）

等宽 tabular 数字 + `caption` 标签，一行三个共享同一基线，**用分隔线绑成一组而不是三张卡片**。

**没有可测的东西时不要显示指标**：给新账号看「0 次运行 / $0.00」是在用一排零占满首屏，
此时空轨道已经把该说的说完了。

### 空态（`EmptyRail`）

画一条**还没有东西经过的轨道**，不用虚线方框。**必须同时说明原因和下一步动作**，且
「还没有任何资源」和「没有匹配的筛选结果」是两种处境，**文案不得复用**：前者是邀请，后者是死路，
要把人带出来。

文案从**用户视角**写，不从系统结构写：是「给 Agent 一件能用的工具」，不是「暂无 Tool 数据」。

### 报错

说清**什么失败了**，并给出重试入口。不道歉、不含糊、不出现没有下文的「出错了」。

### Toast

底部居中，停留 2–3 秒，同时只有一个。**按钮叫什么，toast 就叫什么**（按「发布」→「已发布」）。
只做反馈，不承载需要用户决策的内容。

---

## 四点五、动效规范

**全站只有一处会自己动：human gate 的呼吸，以及首页 hero 那一次轨道推进。**
其余一切只在 hover / focus 时给反馈。这不是保守——散落各处的动效正是"AI 生成感"的主要来源，
而把动效收拢到一个点，才让那个点真的有意义：它在等你。

### 时长与缓动

| 类型 | 时长 | 缓动 | 用在哪 |
|---|---|---|---|
| 微交互 | 150ms | `ease-out` | 按钮 hover、输入框聚焦、颜色过渡 |
| 元素进出 | 200ms | `cubic-bezier(0.16, 1, 0.3, 1)` | 下拉展开、Toast 进出 |
| 弹窗 | 250ms | 同上 | 淡入 + 轻微缩放（0.98→1） |
| 轨道推进 | 500ms/段 | `ease-out` | 首页 hero，段间隔 620ms |
| **gate 呼吸** | 2.4s 循环 | `ease-out` | **仅 human gate**，扩散式 box-shadow |

**超过 500ms 的动效不做。**

### 具体规则

- **卡片 hover**：只加深边框。不上移、不加阴影、不缩放
- **按钮 hover**：只变底色/边框，不改尺寸，避免布局跳动
- **列表项进入**：不做逐条延迟的瀑布式动画
- **骨架屏**：中性灰微光，**绝不用品牌色**
- **页面切换**：不做整页转场

### 无障碍

所有动效包在 `prefers-reduced-motion` 全局规则内（见 `index.css`）。

关闭动效后**状态本身仍要可辨识**：hero 轨道会直接渲染成"已停在 gate"的终态——蓝色轨道到 gate、
橙色站点、之后灰色轨道——而不是留在空白的起始帧。已实测验证。

---

## 五、图标

24×24 线性 SVG，`stroke-width: 1.8`（小对勾 2.6）。标准尺寸 18-21px，特色 24-28px，与相邻文字间距 8-10px。

**不要在同一上下文混用 emoji、图标字体和 SVG 图标**；纯图标控件必须有 `aria-label`。

---

## 六、无障碍

| 项 | 要求 |
|---|---|
| 对比度 | 正文 4.5:1；大字(≥18pt 或 ≥14pt 粗)/图标 3:1；UI 组件边框 3:1 |
| 焦点 | 全局 `:focus-visible` 规则：2px `blueprint` 轮廓 + 2px 偏移，**永远不移除焦点环**。已实测：首页连续 Tab 16 个元素，全部有可见焦点环 |
| 点击区域 | 决策类操作（审批通过/驳回、页面级主 CTA）**≥44px**；日常操作 36px；列表行内次要操作 32px。**已知偏差**：后两者低于 40×40 的通用建议，代价是控制台的信息密度——若后续要改，改 `button.tsx` 的 size 档位一处即可 |
| 键盘 | 完整支持 Tab / Enter / Space / Escape |
| 弹窗 | `role="dialog"` + `aria-modal` + 可读标题 |
| 表单错误 | 通过 `aria-describedby` 关联 |
| 图片 | 有意义的配 `alt` 描述，装饰性的 `alt=""` |
| 状态传达 | **任何状态都不能只靠颜色**，必须同时有文字、图标或形状变化 |

**本平台需额外注意的两处**：

1. **不横向溢出**：375 / 768 / 1440px 三个断点下全部页面 `scrollWidth == clientWidth`，已实测。移动端顶部导航横向滚动，wordmark 文字在 <640px 隐藏让位给导航。
2. **流式输出的屏幕阅读器播报**：Chat 气泡逐字追加内容时，若整段用 `aria-live="polite"` 会导致每追加一个字就重播一次。做法是气泡容器设 `aria-busy="true"`，**节点完成后**再置 `false` 并播报完整产出。
3. **执行图的键盘可达性**：SVG 执行图不能只支持鼠标 hover 查看节点详情，每个节点需可 Tab 聚焦、Enter 展开详情；同时提供「时间线」视图作为等价的纯文本替代路径。

---

## 七、命名约定

设计系统组件用 `ds-` 前缀；业务组件用 `模块-元素--状态`（如 `bundle-card__title`）。状态类统一 `is-active` / `is-open` / `is-loading` / `is-disabled`。

**新的可复用模式必须先加进设计系统再在页面使用**，不能反过来。

---

## 八、本平台新增组件（按 component-spec 方法论推导）

以下组件 tokens 未覆盖，按方法论推导。**新值需合并回 `tokens-pc.md`**。

### 组件：Agent 消息气泡 `ds-agent-bubble`

- **描述**：Chat 界面中承载单个 Agent 产出的容器，支持流式追加内容
- **适用平台**：PC
- **什么时候用**：Bundle 运行过程中展示某个 Agent 的思考与产出
- **什么时候不用**：用户自己的输入（用 `ds-user-bubble`，右对齐、`surface-muted` 底）；系统级提示（用 Toast 或内联提示条）

**结构**：角色头像(32px 圆) + 角色名(`label-md`) + 状态胶囊 + 正文区(`body-lg`) + 工具调用状态条(可选) + 底部操作区(「查看完整产出」链接)

**状态**：

| 状态 | 视觉 |
|---|---|
| 运行中 | 左边框 2px `primary`，状态胶囊「运行中」，正文逐字追加 + 末尾闪烁光标 |
| 已完成 | 左边框 `success`，胶囊「已完成」，显示「查看完整产出」链接 |
| 失败 | 左边框 `error`，胶囊「失败」，正文区改为错误信息（黑盒运行时已脱敏） |
| 黑盒 | 角色名后加「黑盒」chip，不显示思考过程，只显示最终产出 |
| 内容溢出 | 超过 12 行折叠，显示「展开全部」 |

**Token**：底色 `colors.surface`，边框 `colors.border`，圆角 `rounded.lg`，内边距 `space-5`，气泡间距 `space-4`

### 组件：执行图节点 `ds-graph-node`

- **描述**：右侧辅助栏中表示 Bundle 编排图里一个 Agent 节点
- **适用平台**：PC

**状态**：五种节点状态按 §1.2 的语义映射着色，**每种都配图标 + 文字**，不靠纯色区分。黑盒运行时渲染为占位节点（`surface-muted` 底 + 锁图标 + 「黑盒节点」文字，不显示真实节点名）。

**Token**：圆角 `rounded.sm`，节点内边距 `space-3`，节点间距 `space-6`；连线 `colors.border-strong` 1.2px，自环重试边用 `colors.warning` 虚线（3 3）

### 组件：内联审批卡 `ds-gate-card`

- **描述**：human gate 触发时插入 Chat 对话流的审批卡片
- **适用平台**：PC
- **为什么不用 Modal**：审批需要参考上下文对话内容做判断，弹窗会遮挡上下文；且弹窗打断阅读流，与"对话"的产品隐喻冲突

**结构**：警示图标 + 标题(「XX 已完成，需要你确认后继续」) + 关键产出摘要 + 操作区(「驳回」次按钮 + 「通过」主按钮)

**状态**：

| 状态 | 视觉 |
|---|---|
| 待处理（有权限） | `signal` 边框 + `signal-tint` 底；顶部一条「运行已暂停，等你决定」+ **呼吸站点**（与 hero 轨道同一个标记）；两个 44px 按钮 |
| 待处理（无权限） | 同上，按钮替换为「只有发起这次运行的人可以审批」 |
| 已通过 | 折叠为单行状态条 + `moss` 对勾 |
| 已驳回 | 折叠为单行状态条 + `rust` 叉号 |
| 超时 | 折叠为单行 + 说明超时处理结果（自动通过/驳回/中止） |

**Token**：边框与文字 `signal`，底 `signal-tint`，圆角 `rounded-lg`。
呼吸动画 `animate-gate-await` —— **全站唯一自发动效**。

### 组件：广场资源卡 `ds-listing-card`

继承通用卡片规范：类型 `Ref` + 黑盒标记 → 标题 → 摘要（3 行截断）→ 分隔线 → 作者行（头像 20px + 名称 + 版本 + 订阅数）→ 底部操作区（分隔线隔开，未订阅为通栏「订阅 vX」主按钮 / 已订阅为「已订阅」状态 + 「打开」次按钮）。

黑盒标记是**事实陈述不是装饰徽章**：它对订阅者拿到什么是一条真实限制，写清楚比事后惊讶好。

---

## 九、变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-21 | 初版。基于 `tokens-pc.md` 建立；修正上游深色风格与独立登录页两处偏差；新增 4 个平台特有组件规范 |
| v2.0 | 2026-08-24 | **全站 UI/UX 重构**。确立「运行轨道」为签名元素；废除品牌渐变；色板换成纸面/图纸蓝/信号橙六色，信号橙收敛为「需要人介入」唯一含义；三字体分工（Space Grotesk / Inter+Noto / JetBrains Mono），标识符与指标一律等宽；圆角从 8–28px 收到 3–14px；Tab 从填色胶囊改为下划线；空态改用空轨道并按场景重写文案；动效收拢到 hero 推进与 gate 呼吸两处 |
